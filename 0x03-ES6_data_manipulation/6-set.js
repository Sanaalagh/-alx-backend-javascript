/**
 * Returns a Set from an array.
 * 
 * @param {Array} arr - Array of any kind of element.
 * @returns {Set} Set of elements from the array.
 */
function setFromArray(arr) {
  return new Set(arr);
}

export default setFromArray;

